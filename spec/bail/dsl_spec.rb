require 'spec_helper'
require 'bail/dsl.rb'

describe Bail::DSL do
  subject { Bail::DSL.new }

  describe '#' do
    context '' do
      it '' do
        # expect(subject.questionable(:is_a?)).to eq :is_a?
      end
    end
  end
end
